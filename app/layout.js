export const metadata = { title: 'Atlas Capital — Pont MoneyFusion' };

export default function RootLayout({ children }) {
  return (
    <html lang="fr">
      <body>{children}</body>
    </html>
  );
}
